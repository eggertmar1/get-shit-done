---
phase: 04-integration
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - get-shit-done/workflows/complete-milestone.md
  - get-shit-done/workflows/transition.md
  - get-shit-done/workflows/map-codebase.md
  - get-shit-done/workflows/discovery-phase.md
  - get-shit-done/workflows/discuss-phase.md
  - get-shit-done/workflows/verify-phase.md
  - get-shit-done/workflows/verify-work.md
  - get-shit-done/workflows/diagnose-issues.md
  - get-shit-done/workflows/list-phase-assumptions.md
  - .claude/agents/gsd-phase-researcher.md
  - .claude/agents/gsd-project-researcher.md
  - .claude/agents/gsd-research-synthesizer.md
  - .claude/agents/gsd-codebase-mapper.md
  - .claude/agents/gsd-verifier.md
  - .claude/agents/gsd-plan-checker.md
  - .claude/agents/gsd-integration-checker.md
  - .claude/agents/gsd-debugger.md
  - commands/gsd/plan-phase.md
  - commands/gsd/execute-phase.md
  - commands/gsd/research-phase.md
  - commands/gsd/discuss-phase.md
  - commands/gsd/verify-work.md
  - commands/gsd/complete-milestone.md
  - commands/gsd/progress.md
  - commands/gsd/resume-work.md
  - commands/gsd/pause-work.md
  - commands/gsd/debug.md
  - commands/gsd/quick.md
  - commands/gsd/add-phase.md
  - commands/gsd/insert-phase.md
  - commands/gsd/remove-phase.md
  - commands/gsd/add-todo.md
  - commands/gsd/check-todos.md
  - commands/gsd/list-phase-assumptions.md
  - commands/gsd/new-milestone.md
  - commands/gsd/audit-milestone.md
  - commands/gsd/plan-milestone-gaps.md
  - commands/gsd/set-profile.md
  - .planning/projects/multi-project-support/phases/01-foundation-migration/command-integration-checklist.md
autonomous: true

must_haves:
  truths:
    - "All remaining workflows resolve project-specific paths through PROJECT_BASE"
    - "All remaining agents reference path-resolution.md and use PROJECT_BASE for project-specific paths"
    - "All command orchestrators that access project files use PROJECT_BASE resolution"
    - "map-codebase workflow correctly writes to shared .planning/codebase/ (not PROJECT_BASE)"
    - "gsd-codebase-mapper agent writes to shared .planning/codebase/ (not PROJECT_BASE)"
    - "Command integration checklist updated to reflect completion of all 24 items"
    - "Multi-project path resolution works across Claude Code, OpenCode, and Gemini runtimes"
  artifacts:
    - path: "get-shit-done/workflows/complete-milestone.md"
      provides: "Project-aware milestone completion"
      contains: "PROJECT_BASE"
    - path: "get-shit-done/workflows/transition.md"
      provides: "Project-aware plan transition"
      contains: "PROJECT_BASE"
    - path: ".claude/agents/gsd-phase-researcher.md"
      provides: "Project-aware phase researcher"
      contains: "path-resolution"
    - path: ".planning/projects/multi-project-support/phases/01-foundation-migration/command-integration-checklist.md"
      provides: "Completed integration tracking"
      contains: "[x]"
  key_links:
    - from: "commands/gsd/plan-phase.md"
      to: "get-shit-done/workflows/execute-phase.md"
      via: "workflow invocation with project context"
      pattern: "PROJECT_BASE"
    - from: "get-shit-done/workflows/map-codebase.md"
      to: ".planning/codebase/"
      via: "shared path (always root)"
      pattern: "\\.planning/codebase/"
---

<objective>
Complete multi-project path resolution integration across all remaining GSD components: 9 workflows, 8 agents, and ~19 command orchestrators. Update the integration checklist to reflect full completion. Verify cross-runtime compatibility.

Purpose: After Plan 01 integrates the 6 highest-priority components, this plan covers the remaining 18 checklist items plus command orchestrators. This completes INTEG-01 (all commands work with active project context) and INTEG-03 (cross-runtime compatibility).

Output: All 24 integration checklist items marked complete. Every GSD workflow, agent, and command correctly resolves project-specific paths through PROJECT_BASE while preserving shared paths at root.
</objective>

<execution_context>
@/Users/eggert/.claude/get-shit-done/workflows/execute-plan.md
@/Users/eggert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/projects/multi-project-support/PROJECT.md
@.planning/projects/multi-project-support/ROADMAP.md
@.planning/projects/multi-project-support/STATE.md

# References defining the integration patterns
@get-shit-done/references/path-resolution.md
@get-shit-done/references/active-project-validation.md
@get-shit-done/references/shared-paths.md

# Integration checklist with per-component details
@.planning/projects/multi-project-support/phases/01-foundation-migration/command-integration-checklist.md

# Phase 4 research with integration patterns and code examples
@.planning/projects/multi-project-support/phases/04-integration/04-RESEARCH.md

# Plan 01 summary for context on what was already done
@.planning/projects/multi-project-support/phases/04-integration/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate path resolution into remaining 9 workflows and 8 agents</name>
  <files>
    get-shit-done/workflows/complete-milestone.md
    get-shit-done/workflows/transition.md
    get-shit-done/workflows/map-codebase.md
    get-shit-done/workflows/discovery-phase.md
    get-shit-done/workflows/discuss-phase.md
    get-shit-done/workflows/verify-phase.md
    get-shit-done/workflows/verify-work.md
    get-shit-done/workflows/diagnose-issues.md
    get-shit-done/workflows/list-phase-assumptions.md
    .claude/agents/gsd-phase-researcher.md
    .claude/agents/gsd-project-researcher.md
    .claude/agents/gsd-research-synthesizer.md
    .claude/agents/gsd-codebase-mapper.md
    .claude/agents/gsd-verifier.md
    .claude/agents/gsd-plan-checker.md
    .claude/agents/gsd-integration-checker.md
    .claude/agents/gsd-debugger.md
  </files>
  <action>
    Apply the same integration pattern from Plan 01 to all remaining workflows and agents. For each file:

    **A. Add to execution_context:**
    ```
    @get-shit-done/references/path-resolution.md
    @get-shit-done/references/active-project-validation.md
    ```

    **B. Add PROJECT_BASE resolution at process start** (same snippet as Plan 01):
    ```bash
    if [ -d .planning/projects/ ]; then
      ACTIVE_PROJECT=$(cat .planning/.active | tr -d '[:space:]')
      PROJECT_BASE=".planning/projects/$ACTIVE_PROJECT"
    else
      PROJECT_BASE=".planning"
    fi
    ```

    **C. Replace hardcoded .planning/ paths with $PROJECT_BASE/:**
    Same rules: project-specific paths get PROJECT_BASE, shared paths stay at root.

    **SPECIAL CASES:**

    **map-codebase.md** and **gsd-codebase-mapper.md**: These write to `.planning/codebase/` which is SHARED. Do NOT use PROJECT_BASE for codebase paths. Only add path resolution for config.json reading (shared) and any references to PROJECT.md (project-specific, if any). Mark `.planning/codebase/` paths as explicitly shared with a comment.

    **complete-milestone.md** (37 references): Heavy user of .planning/ paths. Reads and writes STATE.md, ROADMAP.md, scans phases/. All project-specific, all need PROJECT_BASE.

    **transition.md** (12 references): Reads STATE.md, ROADMAP.md, phases/*-SUMMARY.md. Writes STATE.md. All project-specific.

    **Agents at ~/.claude/agents/**: Remember these live OUTSIDE the repo. Update at their actual filesystem paths.

    Process files in this order for efficiency:
    1. MEDIUM-priority workflows (complete-milestone, transition, map-codebase, discovery-phase, discuss-phase)
    2. LOW-priority workflows (verify-phase, verify-work, diagnose-issues, list-phase-assumptions)
    3. MEDIUM-priority agents (gsd-phase-researcher, gsd-project-researcher, gsd-research-synthesizer, gsd-codebase-mapper)
    4. LOW-priority agents (gsd-verifier, gsd-plan-checker, gsd-integration-checker, gsd-debugger)
  </action>
  <verify>
    ```bash
    # All workflows should have path-resolution reference
    for f in get-shit-done/workflows/complete-milestone.md get-shit-done/workflows/transition.md get-shit-done/workflows/discovery-phase.md get-shit-done/workflows/discuss-phase.md get-shit-done/workflows/verify-phase.md get-shit-done/workflows/verify-work.md get-shit-done/workflows/diagnose-issues.md get-shit-done/workflows/list-phase-assumptions.md; do
      grep -q "path-resolution" "$f" && echo "OK: $f" || echo "MISSING: $f"
    done

    # map-codebase should NOT have PROJECT_BASE for codebase paths
    grep "PROJECT_BASE.*codebase" get-shit-done/workflows/map-codebase.md && echo "ERROR: codebase should be shared" || echo "OK: codebase paths are shared"

    # All agents should have path-resolution reference
    for f in ~/.claude/agents/gsd-phase-researcher.md ~/.claude/agents/gsd-project-researcher.md ~/.claude/agents/gsd-research-synthesizer.md ~/.claude/agents/gsd-codebase-mapper.md ~/.claude/agents/gsd-verifier.md ~/.claude/agents/gsd-plan-checker.md ~/.claude/agents/gsd-integration-checker.md ~/.claude/agents/gsd-debugger.md; do
      grep -q "path-resolution" "$f" && echo "OK: $f" || echo "MISSING: $f"
    done
    ```
  </verify>
  <done>
    All 9 remaining workflows and 8 remaining agents have path-resolution.md in execution_context, use PROJECT_BASE for project-specific paths, preserve shared paths at root, and maintain flat-structure backwards compatibility.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate path resolution into command orchestrators</name>
  <files>
    commands/gsd/plan-phase.md
    commands/gsd/execute-phase.md
    commands/gsd/research-phase.md
    commands/gsd/discuss-phase.md
    commands/gsd/verify-work.md
    commands/gsd/complete-milestone.md
    commands/gsd/progress.md
    commands/gsd/resume-work.md
    commands/gsd/pause-work.md
    commands/gsd/debug.md
    commands/gsd/quick.md
    commands/gsd/add-phase.md
    commands/gsd/insert-phase.md
    commands/gsd/remove-phase.md
    commands/gsd/add-todo.md
    commands/gsd/check-todos.md
    commands/gsd/list-phase-assumptions.md
    commands/gsd/new-milestone.md
    commands/gsd/audit-milestone.md
    commands/gsd/plan-milestone-gaps.md
    commands/gsd/set-profile.md
  </files>
  <action>
    For each command orchestrator that accesses project-specific `.planning/` files:

    **A. Add active-project-validation.md reference** to the command's execution_context or context section:
    ```
    @get-shit-done/references/active-project-validation.md
    ```

    **B. Add PROJECT_BASE resolution** at the start of the command's process section using the validation snippet.

    **C. Replace hardcoded .planning/ paths** with $PROJECT_BASE/ for project-specific files.

    **Commands to SKIP (already integrated or shared-only):**
    - `new-project.md` — handles own structure detection
    - `switch-project.md` — operates on projects/ directly
    - `list-projects.md` — operates on projects/ directly
    - `archive-project.md` — operates on projects/ directly
    - `map-codebase.md` — writes to shared codebase/
    - `settings.md` — already integrated in Phase 3
    - `help.md` — no file operations
    - `join-discord.md` — no file operations
    - `update.md` — no file operations

    **Per-command notes:**
    - `plan-phase.md` (19 refs): Heavy user of phases/ and STATE.md paths
    - `execute-phase.md` (10 refs): Reads STATE.md, ROADMAP.md, phases/
    - `new-milestone.md` (38 refs): Highest count. Reads/writes PROJECT.md, REQUIREMENTS.md, ROADMAP.md, STATE.md
    - `complete-milestone.md` (11 refs): Reads STATE.md, ROADMAP.md
    - `progress.md` (12 refs): Reads STATE.md, ROADMAP.md, phases/
    - `quick.md` (13 refs): Reads STATE.md, phases/
    - `add-todo.md` (14 refs): Reads/writes todos/ directory
    - `check-todos.md` (13 refs): Reads todos/ directory
    - `set-profile.md` (4 refs): Reads/writes config.json (shared) — only needs resolution for profile storage, not project paths

    IMPORTANT: Command files are user-facing slash command definitions. They are typically shorter than workflows/agents. The integration is lighter — usually just adding the validation reference and replacing a few paths.

    IMPORTANT: For `set-profile.md`, config.json is a SHARED path. Only add path resolution if it also touches project-specific files.
  </action>
  <verify>
    ```bash
    # Spot-check high-reference commands
    for f in commands/gsd/plan-phase.md commands/gsd/new-milestone.md commands/gsd/progress.md commands/gsd/quick.md; do
      grep -q "PROJECT_BASE\|active-project-validation" "$f" && echo "OK: $f" || echo "MISSING: $f"
    done
    ```
  </verify>
  <done>
    All command orchestrators that access project-specific files have been updated with PROJECT_BASE resolution. Commands that only access shared files or handle their own structure detection are left unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update integration checklist and verify cross-runtime compatibility</name>
  <files>
    .planning/projects/multi-project-support/phases/01-foundation-migration/command-integration-checklist.md
  </files>
  <action>
    **A. Update the command integration checklist:**
    Mark all 24 items as complete ([x]) in the checklist file. Update the completion tracking section at the bottom:
    - Completed: 24
    - Progress: [##########] 100%
    - Last Updated: current date

    **B. Cross-runtime compatibility verification:**
    Review all changes made in Tasks 1 and 2 for POSIX compliance:

    1. Check that all bash snippets use `[` not `[[` (POSIX)
    2. Check that `cat file | tr -d` pattern is used (works everywhere)
    3. Check that `ls -1` and `grep` usage is standard
    4. Verify no bashisms (no `${var,,}`, no `[[ ]]`, no process substitution `<()`)
    5. Verify no runtime-specific environment variables are assumed

    If any non-POSIX patterns are found in the integration snippets, fix them.

    **C. Verify the integration pattern works in both structures:**
    Document in the checklist that:
    - Flat structure: PROJECT_BASE=".planning" -> all paths work as before
    - Nested structure: PROJECT_BASE=".planning/projects/<active>" -> project isolation
    - Shared paths: .planning/codebase/, .planning/config.json always at root
    - Error handling: NO_ACTIVE_PROJECT, PROJECT_NOT_FOUND handled consistently
  </action>
  <verify>
    ```bash
    # All checklist items should be marked complete
    grep -c "\[x\]" .planning/projects/multi-project-support/phases/01-foundation-migration/command-integration-checklist.md
    # Expected: 24

    # No unchecked items remain
    grep -c "\[ \]" .planning/projects/multi-project-support/phases/01-foundation-migration/command-integration-checklist.md
    # Expected: 0

    # Verify no [[ ]] bashisms in validation snippets across updated files
    grep -r "\[\[" get-shit-done/workflows/ --include="*.md" -l | head -5
    # Expected: none or only in documentation/examples
    ```
  </verify>
  <done>
    Integration checklist shows 24/24 complete. Cross-runtime compatibility verified — all integration snippets use POSIX-compliant bash. Both flat and nested structures documented as working.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. **Full integration coverage:**
   ```bash
   # Count files with path-resolution reference
   grep -rl "path-resolution" get-shit-done/workflows/ ~/.claude/agents/ commands/gsd/ | wc -l
   # Expected: 30+ files (12 workflows + 11 agents + 19+ commands)
   ```

2. **Checklist completion:**
   ```bash
   grep "Progress:" .planning/projects/multi-project-support/phases/01-foundation-migration/command-integration-checklist.md
   # Expected: 100%
   ```

3. **No broken shared paths:**
   ```bash
   # Codebase paths should never use PROJECT_BASE
   grep -r "PROJECT_BASE.*codebase" get-shit-done/workflows/ commands/gsd/
   # Expected: no matches
   ```

4. **Requirement satisfaction:**
   - INTEG-01: All /gsd:* commands work with active project context (verified by path resolution in all components)
   - INTEG-02: .active gitignored (completed in Plan 01)
   - INTEG-03: Cross-runtime compatible (POSIX compliance verified)
</verification>

<success_criteria>
- All 9 remaining workflows have path-resolution.md reference and use PROJECT_BASE
- All 8 remaining agents have path-resolution.md reference and use PROJECT_BASE
- All ~19 command orchestrators accessing project files use PROJECT_BASE
- map-codebase and gsd-codebase-mapper correctly use shared paths (not PROJECT_BASE)
- Integration checklist shows 24/24 complete
- Cross-runtime compatibility verified (POSIX-compliant snippets)
- INTEG-01, INTEG-02, INTEG-03 requirements all satisfied
</success_criteria>

<output>
After completion, create `.planning/projects/multi-project-support/phases/04-integration/04-02-SUMMARY.md`
</output>
