---
phase: 02-project-commands
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/gsd/new-project.md
autonomous: true

must_haves:
  truths:
    - "User can run /gsd:new-project without arguments and get git branch name as default"
    - "User can run /gsd:new-project [name] to override the default"
    - "Detached HEAD state prompts user for project name instead of failing"
  artifacts:
    - path: "commands/gsd/new-project.md"
      provides: "Git branch default naming in Phase 1.5 and migration flow"
      contains: "git symbolic-ref"
  key_links:
    - from: "commands/gsd/new-project.md"
      to: "git symbolic-ref --short HEAD"
      via: "Bash command for branch detection"
      pattern: "git symbolic-ref"
---

<objective>
Update /gsd:new-project to default project name to current git branch when [name] argument not provided.

Purpose: Users creating projects from feature branches get sensible defaults (e.g., "feature-auth" branch creates "feature-auth" project) without requiring manual name entry every time.

Output: Updated new-project.md with git branch detection and fallback prompting.
</objective>

<execution_context>
@/Users/eggert/.claude/get-shit-done/workflows/execute-plan.md
@/Users/eggert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-project-commands/02-RESEARCH.md
@.planning/phases/02-project-commands/02-CONTEXT.md
@get-shit-done/references/path-resolution.md
@commands/gsd/new-project.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add git branch detection to new-project.md</name>
  <files>commands/gsd/new-project.md</files>
  <action>
Update commands/gsd/new-project.md to add git branch detection for default project naming.

In Phase 1.5 (Create New Project in Nested Structure), BEFORE asking user for project name:

1. Detect if command was called with [name] argument:
   - If argument provided: Use that name directly (skip branch detection)
   - If no argument: Proceed to git branch detection

2. Add git branch detection:
```bash
# Get current git branch (fails in detached HEAD)
BRANCH_NAME=$(git symbolic-ref --short HEAD 2>/dev/null)

# Handle detached HEAD or non-git directory
if [ -z "$BRANCH_NAME" ] || [ "$BRANCH_NAME" == "HEAD" ]; then
  # Cannot auto-detect - must prompt user
  BRANCH_NAME=""
fi
```

3. If BRANCH_NAME detected:
   - Use AskUserQuestion with pre-filled suggestion:
     - header: "New Project"
     - question: "Project name? (detected from git branch)"
     - Show detected branch name as default option
     - Allow override with custom name

4. If no BRANCH_NAME (detached HEAD or non-git):
   - Fall back to existing prompt (ask for name without suggestion)
   - Show helpful message: "Not on a git branch - please provide project name"

5. Apply same logic to Phase 1.3 (migration flow) when asking for project name.

IMPORTANT: Do NOT change any other behavior of new-project.md. Only add the branch detection logic for the project naming step.

Reference Pattern 5 from 02-RESEARCH.md for the git branch detection approach.
  </action>
  <verify>
Read commands/gsd/new-project.md and verify:
- Contains "git symbolic-ref --short HEAD" for branch detection
- Has fallback handling for detached HEAD
- Preserves all existing functionality (migration, nested project creation, brownfield detection)
  </verify>
  <done>
/gsd:new-project without [name] argument defaults to git branch name. Detached HEAD state gracefully prompts for manual name.
  </done>
</task>

<task type="auto">
  <name>Task 2: Commit changes</name>
  <files>commands/gsd/new-project.md</files>
  <action>
Commit the updated new-project.md:

```bash
git add commands/gsd/new-project.md
git commit -m "$(cat <<'EOF'
feat(02-01): add git branch default for project naming

- Detect git branch via symbolic-ref when [name] not provided
- Handle detached HEAD with graceful fallback to manual prompt
- Apply to both migration flow (Phase 1.3) and new project (Phase 1.5)

Requirements: FOUND-01, FOUND-02
EOF
)"
```
  </action>
  <verify>
Run `git log -1 --oneline` to confirm commit was created.
  </verify>
  <done>
Changes committed with descriptive message referencing requirements.
  </done>
</task>

</tasks>

<verification>
After completion, verify:
- [ ] `git symbolic-ref` appears in new-project.md
- [ ] Detached HEAD case has explicit handling
- [ ] All existing new-project.md tests/sections preserved
- [ ] Commit exists with requirements FOUND-01, FOUND-02 referenced
</verification>

<success_criteria>
- new-project.md updated with git branch detection
- Detached HEAD gracefully handled (prompt, not error)
- Changes committed to git
- No regressions in existing new-project functionality
</success_criteria>

<output>
After completion, create `.planning/phases/02-project-commands/02-01-SUMMARY.md`
</output>
